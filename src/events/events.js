import React from 'react';
import EventAlerts from '../events/event_alert';


const Events = () => {
    return (
        <div>
            <EventAlerts />
        </div>
    );
};

export default Events;